import { Cabin, Fraunces, Roboto, Lora } from 'next/font/google'

export const cabin = Cabin({ subsets: ['latin'], variable: '--font-sans' })
export const fraunces = Fraunces({ subsets: ['latin'], variable: '--font-serif' })
export const roboto = Roboto({ subsets: ['latin'], weight: '400', variable: '--font-wall1' })
export const lora = Lora({ subsets: ['latin'], variable: '--font-wall2' })
